{
  "name": "sunrin-app-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js && node --check notification_dispatcher.js && node --check scripts/dispatch-notifications.js",
    "dispatch:notifications": "node scripts/dispatch-notifications.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.0.1"
  }
}
