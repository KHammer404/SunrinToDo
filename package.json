{
  "name": "sunrin-app-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.12.0"
  }
}
